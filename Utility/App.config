<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5" />
    </startup>
    <runtime>
      <gcAllowVeryLargeObjects enabled="true" />
    </runtime>

  <!--  Example for postgtres database, Add Npgsql to references first!
  <system.data>
    <DbProviderFactories>
      <add name="Npgsql Data Provider" invariant="Npgsql"
           description="Data Provider for PostgreSQL"
           type="Npgsql.NpgsqlFactory, Npgsql" />
    </DbProviderFactories>
  </system.data>
  -->
  <connectionStrings>
    <clear />
    <add name="localhost" providerName="System.Data.SqlClient" connectionString="Data Source=localhost;Initial Catalog=eCommerce;Integrated Security=True" />
    <!-- Example for postgres database -->
    <!-- Use 127.0.0.1 instead of localhost, leads to faster connection and no socketexception -->
    <!--<add name="localhost" providerName="Npgsql" connectionString="server=127.0.0.1;database=autoscout24;user id=postgres;password=pentaho;Port=5432" />   -->
  </connectionStrings>

  

</configuration>